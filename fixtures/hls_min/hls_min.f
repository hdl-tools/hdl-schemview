// Minimal HLS designlist (#159/#222). Load with the C source declared:
//   svxprobe-elaborate --top foo -f hls_min.f --hls-map --hls-src foo.cpp -o out.json
// In the app, put `foo.cpp` (or this directory) in the "C/C++ sources" field.
foo.sv
