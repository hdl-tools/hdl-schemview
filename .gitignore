# Rust
/core/target/
target/

# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# Fixture build artifacts (traces under */traces/ are committed; build dirs are not)
fixtures/*/build/
obj_dir/

# Frontend / Tauri app
app/node_modules/
app/dist/
app/src-tauri/target/
app/src-tauri/gen/

# Nix
result
result-*

# Editor / OS
.DS_Store
*.swp
# Added by code-review-graph
.code-review-graph/

# Reference
ref/

# Derived rkyv load cache (#21, ADR 0003) — regenerated from hierarchy.json.
.schemview_data/

# AI cached
# WebView2 fixed-version runtime for the offline Windows bundle (#240). ~180 MB,
# downloaded at build time by the `bundle` CI job (or by hand — see app/README.md)
# and referenced by tauri.offline.conf.json. Never committed.
app/src-tauri/webview2-runtime/

.claude
.gemini
.kiro
.qoder
.vscode
.cursorrules
.mcp.json
.opencode.json
.windsurfrules
AGENTS.md
GEMINI.md
QODER.md